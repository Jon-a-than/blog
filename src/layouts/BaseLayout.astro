---
import { ViewTransitions } from 'astro:transitions'
import HeaderBar from '../components/HeaderBar.astro'
import CCLicense from '../components/CCLicense.astro'

interface Props {
  title: string
}

const { title } = Astro.props
---

<!doctype html>
<html lang="zh">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/@yulania/progress@0.1.3/dist/classic.css"
    />
    <script
      async
      src="https://cdn.jsdelivr.net/npm/@yulania/progress@0.1.3/dist/astro.global.js"
    ></script>
  </head>

  <ViewTransitions />
  <body class="ma-0 scrollbar-slim scroll-smooth">
    <style is:global>
      @import url('../styles/colors.css');

      .yulania-progress {
        --yulania-primary: rgb(var(--color-primary));
      }

      :root {
        color-scheme: light;
        transition:
          background-color 0.2s,
          color 0.2s;

        --uno: 'bg-[url(/texture.png)] bg-var-paper-secondary';

        &[class*='dark'] {
          color-scheme: dark;
        }
      }

      ::selection {
        --uno: 'bg-var-selection';
      }
    </style>
    <HeaderBar />
    <slot />

    <CCLicense />
  </body>
</html>
