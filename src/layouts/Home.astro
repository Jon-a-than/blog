---
import BaseLayout from './BaseLayout.astro'
import '../styles/markdown.css'

interface Props {
  title: string
}

const { title } = Astro.props
---

<BaseLayout title={title}>
  <main class="markdown max-w-2xl mx-auto! mt-16 px-xs">
    <slot />
  </main>
</BaseLayout>
